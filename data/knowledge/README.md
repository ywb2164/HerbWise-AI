# Curated herb knowledge package

Runtime reads `herb_profiles_45.v1.json` for exact name matching and profile display.
The matching CSV is retained for review/import tooling. These records are professional
draft material; clients must display each record's `review_status` and must not present
them as pharmacopoeial final review.
