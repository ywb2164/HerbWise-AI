# Document guidelines

Allowed initial types are PDF, DOCX, plain text, and Markdown. Register files already present in `uploaded_files`; never pass arbitrary server paths. Use `restricted` or `citation_only` copyright states where appropriate.
