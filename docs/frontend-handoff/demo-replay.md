# Soybean Admin replay demo

Replay is an explicit offline source. Badge it as `replay`; it never proves a live RAGFlow or model call. Use safe default demo mode unless an operator explicitly enables real validation.
