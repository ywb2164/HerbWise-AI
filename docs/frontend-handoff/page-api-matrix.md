# Page/API matrix

Login uses `/auth/login`; dashboard/profile uses `/profiles`; identification uses `/files` then `/agent/tasks`; evidence uses `/knowledge/retrieve` and `/knowledge/retrievals`; resources/reviews use `/resources`; learning path uses `/learning-paths`; trace uses `/traces`; report uses `/reports`; administrators use `/admin/knowledge/*`; replay metadata uses `/admin/rag/replays`; metrics uses `/metrics`.
