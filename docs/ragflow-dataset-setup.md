# Dataset setup

Create a Dataset in RAGFlow, obtain its ID and an API key, then configure environment variables. Upload only authorized test material, wait until parsing is `ready`, and confirm the returned document ID, chunk ID, and page metadata. API endpoint compatibility remains pending real-environment confirmation.
